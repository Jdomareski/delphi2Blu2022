unit TestUclasse;
{

  Delphi DUnit Test Case
  ----------------------
  This unit contains a skeleton test case class generated by the Test Case Wizard.
  Modify the generated code to correctly setup and call the methods from the unit 
  being tested.

}

interface

uses
  TestFramework, Uclasse;

type
  // Test methods for class TCompra

  TestTCompra = class(TTestCase)
  strict private
    FCompra: TCompra;
  public
    procedure SetUp; override;
    procedure TearDown; override;
  published
    procedure TestCalcDesconto;
    procedure TestvalorSemDesconto;
    procedure Testdesconto;
  end;

implementation

procedure TestTCompra.SetUp;
begin
  FCompra := TCompra.Create;
end;

procedure TestTCompra.TearDown;
begin
  FCompra.Free;
  FCompra := nil;
end;

procedure TestTCompra.TestCalcDesconto;
var
  ReturnValue: Double;
begin
  fcompra.Quantidade := 10;
  fCompra.Preco := 10;
  ReturnValue := FCompra.CalcDesconto;

  checkEquals(100, returnValue, 'Erro ao calcular desconto');
  // TODO: Validate method results
end;

procedure TestTCompra.TestvalorSemDesconto;
var
  ReturnValue: Double;
begin

  ReturnValue := FCompra.valorSemDesconto;

  // TODO: Validate method results
end;

procedure TestTCompra.Testdesconto;
var
  ReturnValue: string;
begin


  ReturnValue := FCompra.desconto;

  // TODO: Validate method results
end;

initialization
  // Register any test cases with the test runner
  RegisterTest(TestTCompra.Suite);
end.

